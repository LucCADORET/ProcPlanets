#include "procgen/FaceGenerator.hpp"
#include "core/Renderer.h"

class PlanetGenerator {
   public:
    void generatePlanetData(std::vector<VertexAttributes> &vertexData, std::vector<uint32_t> &indices, GUISettings settings);

   private:
};